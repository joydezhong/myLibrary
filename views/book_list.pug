extends layout

block content
  h1= title
  
  ul
  each book in book_list
    li
      a(href=book.url) #{book.title}
      | (#{book.author.family_name}·#{book.author.first_name})
      
  else
    li Three are no books.
